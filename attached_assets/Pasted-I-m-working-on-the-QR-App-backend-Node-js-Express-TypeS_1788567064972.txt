I'm working on the QR App backend (Node.js + Express + TypeScript + Supabase).
Do NOT rewrite anything yet. First AUDIT, then implement on top of what already exists.

NON-NEGOTIABLE CONTEXT (must respect):
- Supabase client separation: supabaseAuth is ONLY for auth operations (getUser, signIn);
  supabaseAdmin (service_role) is ONLY for data queries. Never use the auth client for .from().
- All responses: success { data, message? } / error { error: true, message, code }.
- All endpoints under /api/v1/. Middlewares requireEmployee / requireRole already exist.
- Amounts in Guaraníes = INTEGER, no decimals.
- Soft deletes (is_active=false), never physical DELETE.
- Realtime is already enabled on: payments, table_sessions, restaurant_alerts.
- Global audit_log with levels full/simple.

STEP 1 — AUDIT (report back in text, do NOT change code):
Show me the current logic, summarized and faithful, of these three endpoints:
  a) POST /sessions/:sessionId/close
  b) POST /sessions/:sessionId/invoice  and  GET /sessions/:sessionId/invoice
  c) GET /sessions/:sessionId/payments
For each one state: what it validates, what it writes to the DB, and whether it already rotates
token+PIN or frees the table.
Tell me explicitly whether close is already tested/wired into any flow or was never executed.
Also inspect the enum/constraint on restaurant_alerts.alert_type and tell me which values it accepts today.

STEP 2 — IMPLEMENTATION (only after reporting the audit):

(A) Robust table close — harden POST /sessions/:sessionId/close so that it:
  - Is employees-only (requireEmployee).
  - Validates zero balance using the SAME source of truth as GET /sessions/:sessionId/payments
    (remaining must be 0; order total already with banking-benefit discounts applied, against the sum
    of payments with status='completed', NOT counting tips as part of the order balance).
  - Handles ALL bill-splitting scenarios (equal, "choose mine", custom amount) and the payment links
    for non-connected diners: if someone still has to pay, it does NOT close and returns a clear error
    with the remaining amount and how many participants are still pending.
  - On close: session.status='closed', closed_at=now(), closed_by = employee id.
  - Rotates the table's token and PIN (new values) to invalidate the previous session.
  - Frees the table. If the table belongs to a table group (table_groups / current_group_id),
    free ALL tables in the group and set table_groups.closed_at.
  - Writes to audit_log (full level).

(B) Invoice-triggered close — make sure the panel flow is: (optionally register cash/POS payment) ->
  generate invoice -> close. If cleaner, expose the close as part of the invoice flow, but the zero-balance
  validator from (A) remains the authority. With split invoices (allows_split_invoice), close happens once
  the last invoice is generated and remaining=0.

(C) Digital-payment alert — when POST /payments completes a DIGITAL payment (card/apple_pay/google_pay,
  NOT cash or POS), create a restaurant_alert for the branch signaling "payment received" for the
  table/session, targeted at the waiter role (and the assigned waiter if one exists). Add the value
  'payment_received' to the alert_type enum/constraint (use ALTER TYPE ADD VALUE or modify the CHECK,
  depending on how it's defined today). Do NOT fire an alert for cash/POS payments (the waiter registers
  those himself).

When done: give me a summary of what you changed, in which files, and which SQL migration I must run
in the Supabase SQL Editor (if any). Do NOT touch the tip flow or the banking-benefit flow beyond
reading them for the validation.